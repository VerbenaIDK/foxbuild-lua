#!/bin/sh
source ./project.env
source ./build.env

if $1 != "debug"; then
    ./'build-cpp.sh'
    ./$BUILD_DIR/$PROJECT_NAME-test.elf
else
    ./'build-cpp.sh'
    $DEBUGGER $BUILD_DIR/$PROJECT_NAME-test.elf
fi
